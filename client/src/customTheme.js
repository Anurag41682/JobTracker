import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#f7f7f7",
    },
    primary: {
      main: "#73ff9f",
      darkT: "#113d1c",
      light: "#9dffbc",
    },
  },
});

export default theme;
